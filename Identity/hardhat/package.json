{
  "name": "ssi-hardhat-anchor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "anchor": "hardhat run scripts/anchor.js --network localhost",
    "check": "hardhat run scripts/check.js --network localhost",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "verify:sepolia": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.5",
    "@nomicfoundation/hardhat-verify": "^3.0.11",
    "ethers": "^6.15.0",
    "hardhat": "^3.1.10"
  }
}
